# AI Process - Part 4: 고급 주제

> **AI의 고급 기능들을 프로세스 관점에서 학습하는 교육용 프로젝트**

[![Part](https://img.shields.io/badge/Part-4%20of%204-blue)]()
[![Java](https://img.shields.io/badge/Java-17+-orange)]()
[![Gradle](https://img.shields.io/badge/Gradle-8.5-green)]()

---

## 학습 목표

**RAG, Agent, Fine-tuning, Multimodal 등 고급 AI 기술의 동작 원리를 이해합니다**

```
┌─────────────────────────────────────────────────────────┐
│ STEP 21: RAG (Retrieval-Augmented Generation)           │
│ 문서 청킹 → 임베딩 → 벡터 검색 → 컨텍스트 주입           │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 22: Vector DB                                      │
│ 벡터 저장 → 인덱싱 → 유사도 검색 → 결과 반환             │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 23: Agent (ReAct Pattern)                          │
│ Thought → Action → Observation → 반복 → 종료            │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 24: Fine-tuning (LoRA)                             │
│ 베이스 모델 → 어댑터 학습 → 도메인 특화                  │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 25: Multimodal                                      │
│ 이미지 → 인코딩 → 텍스트 융합 → 통합 분석                │
└─────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 26: Integration                                     │
│ RAG + Agent + Multimodal 통합 데모                       │
└─────────────────────────────────────────────────────────┘
```

---

## 학습 단계 (STEP 21-26)

| STEP | 제목 | 핵심 질문 | 실행 명령 |
|------|------|----------|----------|
| 21 | RAG | 외부 지식을 어떻게 활용하는가? | `./gradlew :step21-rag:run` |
| 22 | Vector DB | 벡터를 어떻게 저장/검색하는가? | `./gradlew :step22-vectordb:run` |
| 23 | Agent | AI가 어떻게 자율적으로 행동하는가? | `./gradlew :step23-agent:run` |
| 24 | Fine-tuning | 모델을 어떻게 특화하는가? | `./gradlew :step24-finetuning:run` |
| 25 | Multimodal | 이미지를 어떻게 이해하는가? | `./gradlew :step25-multimodal:run` |
| 26 | Integration | 모든 기술을 어떻게 통합하는가? | `./gradlew :step26-integration:run` |

---

## 빠른 시작

### 빌드
```bash
./gradlew build
```

### 데모 실행
```bash
# STEP 21: RAG 데모 (핵심)
./gradlew :step21-rag:run

# STEP 23: Agent 데모 (ReAct 패턴)
./gradlew :step23-agent:run

# STEP 26: 통합 데모
./gradlew :step26-integration:run
```

### 출력 예시 (RAG)
```
[ RAG 파이프라인 ]

  1. 문서 청킹:
     "Spring Boot는 자동 설정을 제공합니다..."
     → ["Spring Boot는", "자동 설정을", "제공합니다..."]

  2. 벡터 검색:
     쿼리: "스프링 설정 방법"
     → 유사 문서 3개 검색

  3. 컨텍스트 주입:
     [검색된 문서] + [사용자 질문] → LLM
```

### 출력 예시 (Agent)
```
[ Agent 실행 - ReAct 패턴 ]

  질문: "서울 날씨를 검색해서 우산이 필요한지 알려줘"

  STEP 1:
    Thought: 날씨 정보가 필요합니다
    Action: search("서울 날씨")
    Observation: "서울 오늘 비, 강수확률 80%"

  STEP 2:
    Thought: 비가 오므로 우산이 필요합니다
    Action: finish("우산을 챙기세요!")

  결과: "우산을 챙기세요!" (2단계 완료)
```

### 출력 예시 (Multimodal)
```
[ Multimodal 분석 ]

  입력: 아키텍처 다이어그램 이미지

  분석 결과:
    - 3-tier 아키텍처 감지
    - 컴포넌트: Web, API, DB
    - 연결: 화살표 3개

  생성된 설명:
    "이 다이어그램은 웹, API, 데이터베이스로
     구성된 3계층 아키텍처를 보여줍니다."
```

---

## 프로젝트 구조

```
code-ai-part4-advanced/
├── step21-rag/                # RAG 개념
├── step22-vectordb/           # Vector DB
├── step23-agent/              # Agent (ReAct)
├── step24-finetuning/         # Fine-tuning (LoRA)
├── step25-multimodal/         # Multimodal
├── step26-integration/        # Integration
└── docs/                      # 학습 문서
```

---

## 핵심 개념

### RAG (Retrieval-Augmented Generation)
```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   문서 저장   │ ──→ │  벡터 검색   │ ──→ │  LLM 생성    │
│  (Embedding) │     │  (Semantic)  │     │ (Augmented)  │
└──────────────┘     └──────────────┘     └──────────────┘
```

### Agent (ReAct Pattern)
```
┌─────────────────────────────────────────────────────────┐
│  Thought  →  Action  →  Observation  →  ... →  Finish  │
│   생각         행동        관찰           반복     완료  │
└─────────────────────────────────────────────────────────┘
```

### Fine-tuning (LoRA)
```
┌─────────────────────────────────────────────────────────┐
│ 베이스 모델 (고정)                                       │
│     ↓                                                    │
│ + LoRA 어댑터 (학습)  ← 적은 파라미터로 효율적 학습       │
│     ↓                                                    │
│ 도메인 특화 모델                                         │
└─────────────────────────────────────────────────────────┘
```

### Multimodal
```
┌────────────┐     ┌────────────┐     ┌────────────┐
│   이미지    │ ──→ │  인코더    │ ──→ │   융합     │ ──→ 출력
│            │     │ (패치화)   │     │ (텍스트+)  │
└────────────┘     └────────────┘     └────────────┘
```

---

## Agent 도구

| 도구 | 설명 |
|------|------|
| search | 웹 검색 수행 |
| calculator | 수학 계산 |
| weather | 날씨 조회 |
| finish | 최종 답변 반환 |

---

## 시리즈 전체 구성

```
┌─────────────────────────────────────────────────────────┐
│ Part 1: AI 기초 프로세스 (STEP 1-8)                      │
│ 토큰화 → 컨텍스트 → 확률 → 샘플링 → Embedding → Transformer │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Part 2: 코드 분석 프로세스 (STEP 9-14)                   │
│ 파싱 → AST → 시맨틱 → 패턴매칭 → 이슈탐지 → 스코어링      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Part 3: AI 서비스 프로세스 (STEP 13-18)                  │
│ API → 프롬프트 → LLM → 응답파싱 → 액션 → 피드백           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Part 4: 고급 주제 (STEP 21-26) ◀─────────────────────────  │
│ RAG → VectorDB → Agent → Fine-tuning → Multimodal → 통합 │
└─────────────────────────────────────────────────────────┘
```

---

## 다음 단계

- 👈 [Part 1: AI 기초 프로세스](https://github.com/devload/code-ai-part1-basics)
- 👈 [Part 2: 코드 분석 프로세스](https://github.com/devload/code-ai-part2-analyzer)
- 👈 [Part 3: AI 서비스 프로세스](https://github.com/devload/code-ai-part3-service)

---

## 라이선스

MIT License

---

**Version**: 1.1.0 | **STEP**: 21-26 | **Focus**: Advanced AI Topics
